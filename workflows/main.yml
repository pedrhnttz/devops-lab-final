name: Continuos Integration

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:

      - name: checkout repo content
        uses: actions/checkout@v2

      - name: create docker image
        run: docker build -t meudockerfile .
